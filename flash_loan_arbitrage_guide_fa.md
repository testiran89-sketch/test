# راهنمای عملی آربیتراژ فلش‌لون (CRV/USDC)

این یک راهنمای آموزشی/فنی است و توصیه مالی نیست.

## خلاصه سناریوی شما

طبق داده‌ای که دادی:
- قیمت CRV در **SushiSwap**: `0.2345302003` USDC
- قیمت CRV در **QuickSwap**: `0.2360237348` USDC
- اختلاف خام قیمت ≈ `0.64%`
- با وام 100,000 USDC سود خام تخمینی ≈ `636.32` دلار

ایده اصلی:
1. از پروتکل فلش‌لون (مثلاً Aave V3) مقدار `100,000 USDC` در یک تراکنش قرض می‌گیری.
2. با USDC روی SushiSwap، CRV می‌خری.
3. همان CRV را روی QuickSwap می‌فروشی و USDC پس می‌گیری.
4. اصل و کارمزد فلش‌لون را همان لحظه برمی‌گردانی.
5. باقیمانده USDC، سود تو است.

## قبل از اجرا: شرط سود واقعی

باید این شرط برقرار باشد:

`ProfitNet = USDC_after_sell - LoanPrincipal - FlashFee - DEX_Fees - Gas - SlippageImpact > 0`

اگر حتی یکی از هزینه‌ها بالا برود (مخصوصاً گس و اسلیپیج)، سود 636 دلاری می‌تواند سریعاً صفر یا منفی شود.

## چطور پیاده‌سازی کنی (گام‌به‌گام)

### 1) انتخاب شبکه و تنظیم زیرساخت
- شبکه‌ای را انتخاب کن که **هر دو DEX** و **پروتکل فلش‌لون** روی آن نقدینگی خوب داشته باشند (مثلاً Polygon).
- RPC سریع و پایدار بگیر (ترجیحاً اختصاصی).
- قیمت‌ها را از قرارداد جفت‌ها مستقیم بخوان، نه فقط API عمومی.

### 2) ساخت قرارداد آربیتراژ
یک قرارداد Solidity نیاز داری که:
- از Aave `flashLoanSimple` یا معادل آن استفاده کند.
- در callback فلش‌لون (`executeOperation`) این کارها را انجام دهد:
  1. approve برای SushiSwap
  2. swap `USDC -> CRV` با `amountOutMin` محافظه‌کارانه
  3. approve برای QuickSwap
  4. swap `CRV -> USDC` با `amountOutMin` محافظه‌کارانه
  5. بازپرداخت `amount + premium`
  6. سود داخل کانترکت می‌ماند تا بعداً برداشت کنی
- برای نسخه فعلی این پروژه، شرط سخت‌گیرانه `minProfit` حذف شده و فقط باید بازپرداخت فلش‌لون ممکن باشد.

### 3) محافظت در برابر MEV و frontrun
- تراکنش را از طریق private relay / Flashbots-like channel بفرست.
- `deadline` کوتاه بگذار (مثلاً 60–120 ثانیه).
- `slippage` را سخت‌گیرانه تنظیم کن (مثلاً 10–30 bps بر اساس عمق بازار).

### 4) موتور آف‌چین (Bot)
یک بات آف‌چین لازم داری که:
- هر چند صد میلی‌ثانیه قیمت لحظه‌ای دو DEX را بگیرد.
- با درنظرگرفتن **کارمزد واقعی هر مسیر** سود خالص را حساب کند.
- فقط وقتی `ProfitNet >= threshold` (مثلاً 200 دلار) بود، تابع `executeArb` را صدا بزند.

### 5) تست اجباری قبل از پول واقعی
- ابتدا روی fork شبکه (Hardhat/Foundry mainnet-fork) تست کن.
- سناریوهای بد را تست کن: اسلیپیج ناگهانی، کمبود نقدینگی، revert مسیر دوم.
- با سرمایه خیلی کوچک (مثلاً 1,000 USDC) dry-run واقعی انجام بده.

## نکات عددی مهم برای مثال تو

برای 100,000 USDC:
- اگر کارمزد فلش‌لون 0.09% باشد: `90 USDC`
- اگر هر swap حدود 0.3% کارمزد بگیرد، دو swap می‌شود ~`600 USDC`
- هنوز گس + اسلیپیج را هم باید کم کنی

پس سود خام `636` الزاماً به معنی سود خالص نیست. ممکن است خالص خیلی کمتر (یا منفی) شود.

## چک‌لیست اجرای امن

- [ ] تراکنش فقط وقتی fail شود که بازپرداخت فلش‌لون ممکن نباشد.
- [ ] هر دو swap با `amountOutMin` انجام شوند.
- [ ] مسیرها و توکن‌ها whitelist باشند (جلوگیری از اشتباه آدرس).
- [ ] امکان pause/emergency withdraw داشته باشی.
- [ ] لاگ کامل برای ProfitNet، gasUsed، و دلیل revert ثبت شود.

## جمع‌بندی

در مثال تو «از SushiSwap بخر، روی QuickSwap بفروش» از نظر منطقی درست است،
اما فقط زمانی انجام بده که **بعد از همه هزینه‌ها** سود خالص مثبت بماند.

اگر بخواهی، در مرحله بعد می‌توانم یک اسکلت آماده (Solidity + bot Node.js) بدهم که فقط آدرس‌ها و ABIها را جایگذاری کنی.
